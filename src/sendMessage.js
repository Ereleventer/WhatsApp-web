import { render } from "@testing-library/react";
import React from "react";
import { Users } from "./localDataBase";



function sendMessage(user, theMessage, flag) {
    const chatWith = Users[user].
}